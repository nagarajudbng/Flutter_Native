pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
//        String storageUrl = System.env.FLUTTER_STORAGE_BASE_URL ?: ""
        maven {
            url '/Users/apple/Documents/git_repose/counter_flutter/build/host/outputs/repo'
        }
        maven {
            url "https://storage.googleapis.com/download.flutter.io"
        }
    }
}
rootProject.name = "CourtCount_2"
include ':app'
